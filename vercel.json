{
    "version": 2,
    "builds": [
        {
            "src": "./main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/app/main.py"
        },
        {
          "src": "/api/(.*)",
          "dest": "/app/main.py"
        }
    ]
}
